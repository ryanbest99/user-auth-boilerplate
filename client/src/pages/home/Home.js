import React from "react";
import Layout from "../../containers/layout/Layout";
// import { isAuth } from "../../helpers";

const Home = () => {
  return (
    <div>
      <Layout>
        <h1>Home</h1>
        {/* {JSON.parse(isAuth())} */}
      </Layout>
    </div>
  );
};

export default Home;
